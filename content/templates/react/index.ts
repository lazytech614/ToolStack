import type { Template } from "../types";

import { dashboard } from "./dashboard";
import { portfolio } from "./portfolio";

export { dashboard, portfolio };

export const reactTemplates: Template[] = [dashboard, portfolio];
